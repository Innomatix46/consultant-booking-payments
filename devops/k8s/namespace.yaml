apiVersion: v1
kind: Namespace
metadata:
  name: payment-system
  labels:
    name: payment-system
    compliance: pci-dss
    security.policy: strict
  annotations:
    scheduler.alpha.kubernetes.io/node-selector: "security-zone=pci-compliant"